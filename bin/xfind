#!/bin/sh

parm="";
for par in $@; do
	parm="$parm $par"
done
start=`expr ${#1} + 2`
len=`expr ${#parm} - ${#1}`
key=${parm:$start:$len}

find -E $1 -regex '(Makefile|.*(c|h|py|erl|java|js|jsx|cpp|aidl))' -type f | xargs grep --color=auto -n "${key}"
